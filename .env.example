# Database
DATABASE_URL=postgres://postgres:postgres@db:5432/hrapp

# JWT secret for dev (change in production)
JWT_SECRET=your_jwt_secret

# Optional HuggingFace API key (leave blank to use fallback)
HUGGINGFACE_API_KEY=
